import sqlite3
import json
import os
from telegram import (
    Update, InlineKeyboardButton, InlineKeyboardMarkup, InputFile
)
from telegram.ext import (
    Application, CommandHandler, CallbackQueryHandler,
    MessageHandler, filters, ConversationHandler, ContextTypes
)

# ================== БАЗА ==================
conn = sqlite3.connect("tasks.db", check_same_thread=False)
c = conn.cursor()
c.execute("""CREATE TABLE IF NOT EXISTS tasks (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    user_id INTEGER,
    type TEXT,
    client TEXT,
    order_number TEXT,
    description TEXT,
    address TEXT,
    deadline TEXT,
    avr TEXT,
    solution TEXT,
    status TEXT
)""")
conn.commit()

# ================== СОСТОЯНИЯ ==================
CHOOSING_TYPE, CHOOSING_CLIENT, ENTER_ORDER, ENTER_ADDRESS, ENTER_DESCRIPTION, \
ENTER_DEADLINE, WAITING_AVR, WAITING_SOLUTION = range(8)

# Папка для сохранения файлов (если нужно, создадим)
FILES_DIR = "avr_files"
os.makedirs(FILES_DIR, exist_ok=True)

# ================== КНОПКИ ==================
def main_menu():
    return InlineKeyboardMarkup([
        [InlineKeyboardButton("➕ Новая задача", callback_data="new")],
        [InlineKeyboardButton("📌 Текущие задачи", callback_data="current")],
        [InlineKeyboardButton("📜 История задач", callback_data="history")]
    ])

def type_menu():
    return InlineKeyboardMarkup([
        [InlineKeyboardButton("📝 Общая", callback_data="general"),
         InlineKeyboardButton("🚗 Выездная", callback_data="field")],
        [InlineKeyboardButton("⬅️ Назад", callback_data="back_main")]
    ])

def client_menu():
    return InlineKeyboardMarkup([
        [InlineKeyboardButton("🏢 Максавит", callback_data="Максавит"),
         InlineKeyboardButton("🏭 Газпромнефть", callback_data="Газпромнефть")],
        [InlineKeyboardButton("👗 Глория Джинс", callback_data="Глория Джинс"),
         InlineKeyboardButton("🛒 Перекресток", callback_data="Перекресток")],
        [InlineKeyboardButton("⛽ Тебойл", callback_data="Тебойл"),
         InlineKeyboardButton("⛽ Татнефть", callback_data="Татнефть")],
        [InlineKeyboardButton("⬅️ Назад", callback_data="back_main")]
    ])

def confirm_delete_kb(tid):
    return InlineKeyboardMarkup([
        [InlineKeyboardButton("❌ Подтвердить удаление", callback_data=f"confirm_delete_{tid}")],
        [InlineKeyboardButton("⬅️ Отмена", callback_data=f"open_{tid}")]
    ])

# ================== ОБРАБОТЧИКИ ==================
async def start(update: Update, context: ContextTypes.DEFAULT_TYPE):
    await update.message.reply_text("👋 Привет! Я бот для управления задачами.", reply_markup=main_menu())

# ---- Главное меню ----
async def menu_handler(update: Update, context: ContextTypes.DEFAULT_TYPE):
    query = update.callback_query
    await query.answer()
    if query.data == "new":
        await query.message.edit_text("Выберите тип задачи:", reply_markup=type_menu())
        return CHOOSING_TYPE

# ---- Текущие задачи ----
async def show_current(update: Update, context: ContextTypes.DEFAULT_TYPE):
    query = update.callback_query
    await query.answer()
    c.execute("SELECT id, type, client, address, description FROM tasks WHERE status='active' ORDER BY id DESC")
    tasks = c.fetchall()
    if not tasks:
        await query.message.reply_text("📭 Нет текущих задач.", reply_markup=main_menu())
        return
    kb = []
    for t in tasks:
        tid, ttype, client, addr, desc = t
        if ttype == "field":
            label = f"#{tid} {client} | {addr}"
        else:
            label = f"#{tid} {desc[:30]}..."
        kb.append([InlineKeyboardButton(f"🔎 {label}", callback_data=f"open_{tid}")])
    kb.append([InlineKeyboardButton("⬅️ Назад", callback_data="back_main")])
    await query.message.reply_text("📌 Текущие задачи:", reply_markup=InlineKeyboardMarkup(kb))

# ---- История ----
async def show_history(update: Update, context: ContextTypes.DEFAULT_TYPE):
    query = update.callback_query
    await query.answer()
    c.execute("SELECT id, type, client, address, description FROM tasks WHERE status='done' ORDER BY id DESC LIMIT 10")
    tasks = c.fetchall()
    if not tasks:
        await query.message.reply_text("📭 История пуста.", reply_markup=main_menu())
        return
    kb = []
    for t in tasks:
        tid, ttype, client, addr, desc = t
        if ttype == "field":
            label = f"#{tid} {client} | {addr}"
        else:
            label = f"#{tid} {desc[:30]}..."
        kb.append([InlineKeyboardButton(f"📜 {label}", callback_data=f"open_{tid}")])
    kb.append([InlineKeyboardButton("⬅️ Назад", callback_data="back_main")])
    await query.message.reply_text("📜 История завершённых задач:", reply_markup=InlineKeyboardMarkup(kb))

# ---- Назад ----
async def back_to_main(update: Update, context: ContextTypes.DEFAULT_TYPE):
    query = update.callback_query
    await query.answer()
    await query.message.edit_text("🏠 Главное меню:", reply_markup=main_menu())
    return ConversationHandler.END

# ---- Создание задач ----
async def choose_type(update: Update, context: ContextTypes.DEFAULT_TYPE):
    query = update.callback_query
    context.user_data["type"] = query.data
    await query.answer()
    if query.data == "field":
        await query.message.edit_text("Выберите клиента:", reply_markup=client_menu())
        return CHOOSING_CLIENT
    else:
        await query.message.edit_text("Введите описание задачи:")
        return ENTER_DESCRIPTION

async def choose_client(update: Update, context: ContextTypes.DEFAULT_TYPE):
    query = update.callback_query
    context.user_data["client"] = query.data
    await query.answer()
    await query.message.edit_text("Введите номер заявки:")
    return ENTER_ORDER

async def enter_order(update: Update, context: ContextTypes.DEFAULT_TYPE):
    context.user_data["order_number"] = update.message.text
    await update.message.reply_text("Введите адрес:")
    return ENTER_ADDRESS

async def enter_address(update: Update, context: ContextTypes.DEFAULT_TYPE):
    context.user_data["address"] = update.message.text
    await update.message.reply_text("Введите описание задачи:")
    return ENTER_DESCRIPTION

async def enter_description(update: Update, context: ContextTypes.DEFAULT_TYPE):
    context.user_data["description"] = update.message.text
    await update.message.reply_text("Введите срок (ДД.ММ.ГГ ЧЧ:ММ):")
    return ENTER_DEADLINE

# ---- Ввод срока ----
async def enter_deadline(update: Update, context: ContextTypes.DEFAULT_TYPE):
    context.user_data["deadline"] = update.message.text
    ud = context.user_data

    c.execute("""INSERT INTO tasks (user_id, type, client, order_number, description, address, deadline, status)
                 VALUES (?, ?, ?, ?, ?, ?, ?, ?)""",
              (update.effective_user.id, ud.get("type"), ud.get("client"),
               ud.get("order_number"), ud.get("description"), ud.get("address"),
               ud.get("deadline"), "active"))
    conn.commit()

    context.user_data.clear()
    await update.message.reply_text("✅ Задача добавлена в текущие!", reply_markup=main_menu())
    return ConversationHandler.END

# ---- Открыть задачу ----
async def open_task(update: Update, context: ContextTypes.DEFAULT_TYPE):
    query = update.callback_query
    await query.answer()
    tid = int(query.data.split("_")[1])
    c.execute("SELECT * FROM tasks WHERE id=?", (tid,))
    row = c.fetchone()
    if row:
        _, user_id, ttype, client, order, desc, addr, deadline, avr, sol, status = row
        msg = (
            f"🆔 <b>Задача #{tid}</b>\n"
            f"📌 <b>Тип:</b> {'🚗 Выездная' if ttype == 'field' else '📝 Общая'}\n"
        )
        if client: msg += f"🏢 <b>Клиент:</b> {client}\n"
        if order: msg += f"№ <b>Заявки:</b> {order}\n"
        msg += f"📝 <b>Описание:</b> {desc}\n"
        if addr:
            msg += f"📍 <b>Адрес:</b> {addr}\n🔗 <a href='https://yandex.ru/maps/?text={addr}'>Открыть в Яндекс.Картах</a>\n"
        msg += f"⏰ <b>Срок:</b> {deadline}\n"
        msg += f"📊 <b>Статус:</b> {'✅ Завершена' if status == 'done' else '🕒 Активна'}\n"

        kb = []
        if status == "done":
            if sol: msg += f"📝 <b>Решение:</b> {sol}\n"
            if avr:
                try:
                    avr_files = json.loads(avr)
                    for f in avr_files:
                        # f is dict {"path": ..., "name": ...}
                        await query.message.reply_document(
                            document=InputFile(f["path"]),
                            caption=f"📎 {f['name']}"
                        )
                except Exception:
                    pass
        else:
            kb.append([InlineKeyboardButton("✅ Завершить", callback_data=f"complete_{tid}")])

        kb.append([InlineKeyboardButton("🗑 Удалить", callback_data=f"delete_{tid}")])
        kb.append([InlineKeyboardButton("⬅️ Назад", callback_data="back_main")])

        await query.message.reply_text(msg, reply_markup=InlineKeyboardMarkup(kb),
                                       parse_mode="HTML", disable_web_page_preview=True)

# ---- Завершение ----
async def complete_task(update: Update, context: ContextTypes.DEFAULT_TYPE):
    query = update.callback_query
    await query.answer()
    tid = int(query.data.split("_")[1])
    context.user_data["complete_id"] = tid
    context.user_data["avr_files"] = []
    # Отправляем инструкцию + кнопку "Готово"
    kb = InlineKeyboardMarkup([
        [InlineKeyboardButton("🇬 Готово", callback_data=f"avr_done_{tid}")],
        [InlineKeyboardButton("⬅️ Отмена", callback_data=f"open_{tid}")]
    ])
    await query.message.reply_text(
        "📎 Прикрепите файлы АВР (pdf/jpg/png/heic).\n"
        "Можно несколько подряд. Когда закончите — нажмите кнопку «Готово».",
        reply_markup=kb
    )
    return WAITING_AVR

# Обработчик нажатия кнопки "Готово"
async def avr_done_callback(update: Update, context: ContextTypes.DEFAULT_TYPE):
    query = update.callback_query
    await query.answer()
    # callback_data = avr_done_<tid>
    parts = query.data.split("_")
    if len(parts) >= 3:
        tid = int(parts[2])
    else:
        tid = context.user_data.get("complete_id")
    # Получаем список загруженных во время сессии файлов
    avr_files = context.user_data.get("avr_files", [])
    # Сохраняем в БД (JSON)
    try:
        avr_json = json.dumps(avr_files, ensure_ascii=False)
        c.execute("UPDATE tasks SET avr=? WHERE id=?", (avr_json, tid))
        conn.commit()
    except Exception as e:
        await query.message.reply_text("❌ Ошибка при сохранении файлов. Попробуйте ещё раз.", reply_markup=main_menu())
        context.user_data.clear()
        return ConversationHandler.END

    # Переходим к вводу решения
    context.user_data["complete_id"] = tid
    await query.message.reply_text("Теперь опишите решение (коротко):")
    return WAITING_SOLUTION

# Обработчик "Готово" через текст (на случай, если пользователь напишет слово)
# (receive_avr уже поддерживал текст "готово" — но теперь логично обрабатывать кнопку в conv)
# ---- Приём АВР-файлов (документы и фото) ----
async def receive_avr(update: Update, context: ContextTypes.DEFAULT_TYPE):
    tid = context.user_data.get("complete_id")
    if not tid:
        await update.message.reply_text("❌ Внутренняя ошибка: id задачи не найден.", reply_markup=main_menu())
        context.user_data.clear()
        return ConversationHandler.END

    allowed_ext = (".pdf", ".jpg", ".jpeg", ".png", ".heic")

    # Документ
    if update.message.document:
        file = update.message.document
        tg_file = await file.get_file()
        filename = file.file_name or f"avr_{tid}"
        if not filename.lower().endswith(allowed_ext):
            await update.message.reply_text("⚠️ Разрешены только PDF/JPG/PNG/HEIC.")
            return WAITING_AVR
        # создаём путь в папке
        safe_name = filename.replace("/", "_").replace("\\", "_")
        path = os.path.join(FILES_DIR, f"avr_{tid}_{safe_name}")
        await tg_file.download_to_drive(path)
        # сохраняем dict
        context.user_data.setdefault("avr_files", []).append({"path": path, "name": filename})
        await update.message.reply_text(f"✅ Файл {filename} сохранён. Нажмите кнопку «Готово», когда закончите.")
        return WAITING_AVR

    # Фото
    if update.message.photo:
        photo = update.message.photo[-1]
        tg_file = await photo.get_file()
        # используем unique id для имени
        filename = f"photo_{tid}_{photo.file_unique_id}.jpg"
        path = os.path.join(FILES_DIR, filename)
        await tg_file.download_to_drive(path)
        context.user_data.setdefault("avr_files", []).append({"path": path, "name": filename})
        await update.message.reply_text("✅ Фото сохранено. Нажмите кнопку «Готово», когда закончите.")
        return WAITING_AVR

    # Текст "готово" (на случай, если пользователь напишет)
    if update.message.text and update.message.text.strip().lower() in ("готово", "готово."):
        # Сохраняем список в БД
        avr_json = json.dumps(context.user_data.get("avr_files", []), ensure_ascii=False)
        c.execute("UPDATE tasks SET avr=? WHERE id=?", (avr_json, tid))
        conn.commit()
        await update.message.reply_text("Теперь опишите решение (коротко):")
        return WAITING_SOLUTION

    # Любой другой ввод
    await update.message.reply_text("Пожалуйста, прикрепите файл (pdf/jpg/png/heic) или нажмите кнопку «Готово».")
    return WAITING_AVR

# ---- Приём решения ----
async def receive_solution(update: Update, context: ContextTypes.DEFAULT_TYPE):
    tid = context.user_data.get("complete_id")
    if not tid:
        await update.message.reply_text("❌ Внутренняя ошибка: id задачи не найден.", reply_markup=main_menu())
        context.user_data.clear()
        return ConversationHandler.END
    sol = update.message.text
    c.execute("UPDATE tasks SET solution=?, status=? WHERE id=?", (sol, "done", tid))
    conn.commit()
    context.user_data.clear()
    await update.message.reply_text("✅ Задача завершена и перенесена в историю!", reply_markup=main_menu())
    return ConversationHandler.END

# ---- Удаление ----
async def delete_task(update: Update, context: ContextTypes.DEFAULT_TYPE):
    query = update.callback_query
    await query.answer()
    tid = int(query.data.split("_")[1])
    kb = InlineKeyboardMarkup([
        [InlineKeyboardButton("❌ Подтвердить удаление", callback_data=f"confirm_delete_{tid}")],
        [InlineKeyboardButton("⬅️ Отмена", callback_data=f"open_{tid}")]
    ])
    await query.message.reply_text(f"⚠️ Удалить задачу #{tid}?", reply_markup=kb)

async def confirm_delete(update: Update, context: ContextTypes.DEFAULT_TYPE):
    query = update.callback_query
    await query.answer()
    tid = int(query.data.split("_")[2])
    c.execute("DELETE FROM tasks WHERE id=?", (tid,))
    conn.commit()
    await query.message.reply_text(f"🗑 Задача #{tid} удалена.", reply_markup=main_menu())

# ================== MAIN ==================
def main():
    TOKEN = "8288052246:AAH2HkB5MVqBA8M6gINmLCap0wZ7Fyn8jKY"

    app = Application.builder().token(TOKEN).build()
    app.bot.delete_webhook(drop_pending_updates=True)

    conv = ConversationHandler(
        entry_points=[CallbackQueryHandler(menu_handler, pattern="^new$")],
        states={
            CHOOSING_TYPE: [CallbackQueryHandler(choose_type, pattern="^(general|field)$")],
            CHOOSING_CLIENT: [CallbackQueryHandler(choose_client)],
            ENTER_ORDER: [MessageHandler(filters.TEXT & ~filters.COMMAND, enter_order)],
            ENTER_ADDRESS: [MessageHandler(filters.TEXT & ~filters.COMMAND, enter_address)],
            ENTER_DESCRIPTION: [MessageHandler(filters.TEXT & ~filters.COMMAND, enter_description)],
            ENTER_DEADLINE: [MessageHandler(filters.TEXT & ~filters.COMMAND, enter_deadline)],
            WAITING_AVR: [
                MessageHandler(filters.Document.ALL, receive_avr),
                MessageHandler(filters.PHOTO, receive_avr),
                MessageHandler(filters.TEXT & ~filters.COMMAND, receive_avr),
                CallbackQueryHandler(avr_done_callback, pattern=r"^avr_done_\d+$"),  # кнопка "Готово"
            ],
            WAITING_SOLUTION: [MessageHandler(filters.TEXT & ~filters.COMMAND, receive_solution)],
        },
        fallbacks=[CallbackQueryHandler(back_to_main, pattern="^back_main$")],
    )

    app.add_handler(CommandHandler("start", start))
    app.add_handler(conv)

    # глобальные кнопки
    app.add_handler(CallbackQueryHandler(show_current, pattern="^current$"))
    app.add_handler(CallbackQueryHandler(show_history, pattern="^history$"))
    app.add_handler(CallbackQueryHandler(back_to_main, pattern="^back_main$"))

    # задачи
    app.add_handler(CallbackQueryHandler(open_task, pattern=r"^open_\d+$"))
    app.add_handler(CallbackQueryHandler(complete_task, pattern=r"^complete_\d+$"))
    app.add_handler(CallbackQueryHandler(delete_task, pattern=r"^delete_\d+$"))
    app.add_handler(CallbackQueryHandler(confirm_delete, pattern=r"^confirm_delete_\d+$"))

    app.run_polling()

if __name__ == "__main__":
    main()
